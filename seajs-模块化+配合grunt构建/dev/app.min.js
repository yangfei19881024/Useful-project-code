define("app/app", [ "jquery", "../drag/drag", "../range/range" ], function(require, exports, module) {
    var $ = require("jquery");
    alert($);
    var box = document.getElementById("box"), drag = require("../drag/drag");
    drag.drag(box);
}), define("drag/drag", [ "../range/range" ], function(require, exports, module) {
    function drag(obj) {
        document.onmousedown = function(event) {
            var startx = event.clientX - obj.offsetLeft, starty = event.clientY - obj.offsetTop;
            document.onmousemove = function(event) {
                var leftx = event.clientX - startx, lefty = event.clientY - starty, L = require("../range/range").range(leftx, document.documentElement.clientWidth - obj.offsetWidth, 0), T = require("../range/range").range(lefty, document.documentElement.clientHeight - obj.offsetHeight, 0);
                obj.style.left = L + "px", obj.style.top = T + "px";
            }, document.onmouseup = function() {
                document.onmousemove = null, document.onmouseup = null;
            };
        };
    }
    exports.drag = drag;
}), define("range/range", [], function(require, exports, module) {
    function range(val, max, min) {
        return val = val >= max ? max : min >= val ? min : val;
    }
    exports.range = range;
});